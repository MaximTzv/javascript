
function findOccurence([sentence, word]) {

    sentence = sentence.toLowerCase();
    word = word.toLowerCase();
    let count = 0;
    let regex = /[A-Za-z0-9]+/g;

    sentence.filter()
}

console.log(findOccurence(['There was one. Therefore I bought it. I wouldn’t buy it otherwise.', 'there']));
